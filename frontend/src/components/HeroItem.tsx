import styled from "styled-components";

export default function HeroItem() {
    return (
        <Wrapper>

        </Wrapper>
    );
};

const Wrapper = styled.div`
    width: 1440px;
    height: 797px;
`
