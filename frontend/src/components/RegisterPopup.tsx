import styled from "styled-components";

export default function RegisterPopup() {
    return (
        <Wrapper>
            
        </Wrapper>
    );
};

const Wrapper = styled.div`
    max-width: 410px;
    max-height: 415px;
`
